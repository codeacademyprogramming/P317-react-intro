export const add = (a, b) => {
  return a + b;
};

export const substract = (a, b) => {
  return "";
};

export default class CustomMath {
  add(a, b) {
    return a + b;
  }
  substract(a, b) {
    return a - b;
  }
}
